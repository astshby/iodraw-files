```mermaid
graph TD
    A[Start: 初始化模型参数和计数器] --> B(设置循环计数器 i = 0)
    B --> C{是否处理完所有图片? (i < 100)}
    C -- 是 --> D(加载第 i 张图片数据到 image 数组)
    D --> E(调用 forward_propagation: CNN 推理核心)
    E --> F[CNN 前向传播内部流程: Conv -> Pool -> Flatten -> Dense -> Softmax]
    F --> G(获取预测结果 P 和 Softmax 概率)
    G --> H{预测结果 P == 真实标签 ans[i] ?}
    H -- 是 --> I(正确计数 correct_count + 1)
    H -- 否 --> J(输出错误信息和预测值)
    I --> K(循环计数 i + 1)
    J --> K
    K --> C
    C -- 否 --> L(计算并输出总准确率)
    L --> M[End: 程序终止]
    
```